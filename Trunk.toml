[build]
# Output directory for build artifacts
dist = "dist"

[tools]
# wasm-opt disabled - Cargo profile handles LTO/strip/opt-level=z

# wasm-bindgen needs to match the version in Cargo.lock
wasm_bindgen = "0.2.106"

[watch]
# Files to ignore during watch mode
ignore = ["dist", "target"]

# GitHub Pages deployment configuration
# Set public_url via CLI: trunk build --release --public-url /walkie-songie/
# Or override here for a specific deployment target
