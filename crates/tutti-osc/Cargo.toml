[package]
name = "tutti-osc"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.1"

[dependencies]
# The music protocol: the view this bridge projects and the fractional-MIDI
# resolution its floats carry. Nothing else — the codec is hand-rolled and the
# core is sans-io (sockets stay in the app's driver).
tutti-music = { path = "../tutti-music" }
