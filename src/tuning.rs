//! Walkie's tuning vocabulary — the whole surface of [`tutti_music::tuning`],
//! re-exported so every `crate::tuning::…` spelling keeps compiling now that the
//! module lives in the music protocol crate (tutti Phase II, roadmap §A.3.2).

pub use tutti_music::tuning::*;
